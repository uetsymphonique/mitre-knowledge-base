# systemd

1 unique events

| Log Source | Channel | Data Components |
|------------|---------|-----------------|
| `systemd:unit` | container run with restart policy set to 'always' or 'unless-stopped' | Container Creation |
